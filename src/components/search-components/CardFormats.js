import React from 'react';
import PropTypes from 'prop-types';

function CardFormats({ formats }) {
  return ();
}

CardFormats.propTypes = {
  formats: PropTypes.array.isRequired
};

export default CardFormats;
